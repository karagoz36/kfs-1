# GRUB konfigurasyonu: ISO'nun /boot/grub/grub.cfg yoluna kopyalanir.
# 'multiboot' komutu, kernel'i multiboot 1 protokolu ile yukler.

set timeout=0
set default=0

menuentry "KFS-1" {
	multiboot /boot/kernel.bin
	boot
}
